import { VirtualScroll } from "../src/components/virtual-scroll/virtual-scroll.component";

describe("A test spec", () => {

    it("should pass", () => {
        expect(VirtualScroll).toBeTruthy();
    });
});